#!/bin/bash
set -euo pipefail
IFS=$'\n\t'
script_dir=`dirname $(readlink -f $0)`
# Do any other automated setup that you need to do here
cd `dirname "$script_dir"` && bundle install --path .bundle

# Generate candibox certificate and key
cd $script_dir && ruby setup.rb
