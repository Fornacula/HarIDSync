#!/bin/bash
set -euo pipefail
IFS=$'\n\t'
script_dir=`dirname $(readlink -f $0)`
# Install all Ruby dependencies into .bundle folder
cd `dirname "$script_dir"` && bundle install --path .bundle

# Generate candibox private key and default settings 
echo cd `dirname "$script_dir"` && bin/candibox_sync setup
